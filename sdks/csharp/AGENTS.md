# C# CLIENT - AI AGENT INSTRUCTIONS

## ⚠️ CRITICAL: CHECK YOUR REPOSITORY FIRST

Before making ANY changes, verify you're in the correct repository:

```bash
git remote -v
```

- ✅ **CORRECT**: `origin .../algolia/api-clients-automation.git` → You may proceed
- ❌ **WRONG**: `origin .../flapjackhq/flapjack-search-client-csharp.git` → STOP! This is the PUBLIC repository

**If you're in `flapjacksearch-client-csharp`**: Do NOT make changes here. All changes must go through `api-clients-automation`. PRs and commits made directly to the public repo will be discarded on next release.

## ⚠️ BEFORE ANY EDIT: Check If File Is Generated

Before editing ANY file, verify it's hand-written by checking `config/generation.config.mjs`:

```javascript
// In generation.config.mjs - patterns WITHOUT '!' are GENERATED (do not edit)
'clients/flapjacksearch-client-csharp/**',                              // Generated by default
'!clients/flapjacksearch-client-csharp/flapjacksearch/Clients/FlapjackConfig.cs',  // Hand-written ✓
'!clients/flapjacksearch-client-csharp/flapjacksearch/Exceptions/**',    // Hand-written ✓
'!clients/flapjacksearch-client-csharp/flapjacksearch/Serializer/**',    // Hand-written ✓
'!clients/flapjacksearch-client-csharp/flapjacksearch/Utils/**',         // Hand-written ✓
'!clients/flapjacksearch-client-csharp/flapjacksearch/Http/**',          // Hand-written ✓
'!clients/flapjacksearch-client-csharp/flapjacksearch/Transport/**',     // Hand-written ✓
'!clients/flapjacksearch-client-csharp/flapjacksearch/Models/Common/**', // Hand-written ✓
```

**Hand-written (safe to edit):**

- `flapjacksearch/Clients/FlapjackConfig.cs` - Configuration base
- `flapjacksearch/Exceptions/**` - Exception types
- `flapjacksearch/Serializer/**` - JSON serialization
- `flapjacksearch/Utils/**` - Utility classes
- `flapjacksearch/Http/**` - HTTP client wrapper
- `flapjacksearch/Transport/**` - Transport layer, retry logic
- `flapjacksearch/Models/Common/**` - Common model types

**Generated (DO NOT EDIT):**

- `flapjacksearch/Clients/*Client.cs` (except FlapjackConfig.cs)
- `flapjacksearch/Models/**` (except Models/Common/\*\*)
- Root project files

## Language Conventions

### Naming

- **Files**: `PascalCase.cs` matching class name
- **Classes/Interfaces/Enums**: `PascalCase`
- **Methods/Properties**: `PascalCase`
- **Private fields**: `_camelCase`
- **Parameters/Local variables**: `camelCase`
- **Constants**: `PascalCase`

### Formatting

- .NET default formatting
- Run: `yarn cli format csharp clients/flapjacksearch-client-csharp`

### C# Patterns

- Use `async/await` for asynchronous operations
- Nullable reference types enabled (C# 8+)
- Use `var` for local variables when type is obvious
- LINQ for collections
- Pattern matching in switch expressions

### Dependencies

- **HTTP**: HttpClient (System.Net.Http)
- **JSON**: System.Text.Json
- **Build**: .NET SDK / MSBuild
- **Target**: .NET 8.0+

## Client Patterns

### Transport Architecture

```csharp
// Transport/ - HTTP layer with retry
public class HttpTransport : ITransport
{
    private readonly HttpClient _httpClient;
    private readonly RetryStrategy _retryStrategy;

    public async Task<T> ExecuteAsync<T>(HttpRequest request)
    {
        // Retry logic with host failover
    }
}
```

### Async Pattern

```csharp
// All API methods have async versions
public async Task<SearchResponse> SearchAsync(SearchParams searchParams)

// Usage
var response = await client.SearchAsync(searchParams);

// With cancellation
var response = await client.SearchAsync(searchParams, cancellationToken);
```

### Exception Hierarchy

```csharp
// Exceptions/
public class FlapjackException : Exception { }
public class FlapjackApiException : FlapjackException
{
    public int HttpStatusCode { get; }
    public string HttpBody { get; }
}
public class AlgoliaUnreachableHostsException : FlapjackException { }
```

## Common Gotchas

### Async/Await

```csharp
// WRONG - blocking on async
var response = client.SearchAsync(params).Result;  // Deadlock risk!

// CORRECT - await properly
var response = await client.SearchAsync(params);

// If you must block (avoid), use
var response = client.SearchAsync(params).GetAwaiter().GetResult();
```

### Nullable Reference Types

```csharp
// C# 8+ nullable annotations
string? nullable = null;      // Can be null
string nonNull = "";          // Cannot be null

// Null-conditional
var length = nullable?.Length;

// Null-coalescing
var value = nullable ?? "default";

// Null-forgiving (use sparingly)
var sure = nullable!;
```

### Disposal

```csharp
// Clients are disposable - use using statement
using var client = new SearchClient("APP_ID", "API_KEY");
await client.SearchAsync(params);

// Or explicit disposal
var client = new SearchClient("APP_ID", "API_KEY");
try
{
    await client.SearchAsync(params);
}
finally
{
    client.Dispose();
}
```

### LINQ

```csharp
// Use LINQ for collections
var filtered = response.Hits
    .Where(h => h.Score > 0.5)
    .Select(h => h.ObjectID)
    .ToList();
```

### Serialization

```csharp
// Models use System.Text.Json
[JsonPropertyName("objectID")]
public string ObjectId { get; set; }

// Custom converters in Serializer/
```

## Build & Test Commands

```bash
# From repo root (api-clients-automation)
yarn cli build clients csharp                  # Build C# client
yarn cli cts generate csharp                   # Generate CTS tests
yarn cli cts run csharp                        # Run CTS tests
yarn cli format csharp clients/flapjacksearch-client-csharp

# From client directory
cd clients/flapjacksearch-client-csharp
dotnet build                                   # Build solution
dotnet test                                    # Run tests
dotnet format                                  # Format code
```
